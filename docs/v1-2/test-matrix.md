# Features Tests v1-2

| Feature ID | Unit | Integration | E2E | Status |
| --- | --- | --- | --- | --- |
| graph-store-001 | `ui/apps/server/src/optidevMemory.test.ts` validates the native memory model contract, typed entities, and relation storage shape | `ui/apps/server/src/optidevCliShim.test.ts` exercises memory lookups through the shipped `scripts/optid` entrypoint | `ui/apps/web/src/routes/-optidev.browser.tsx` keeps embedded memory behavior stable inside `/optidev` | PASSED |
| graph-sqlite-001 | `ui/apps/server/src/optidevMemory.test.ts` validates native persisted memory reads, release links, and digest inputs | `ui/apps/server/src/optidevCliShim.test.ts` validates memory commands through the real Bun CLI shim | `ui/apps/web/src/routes/-optidev.browser.tsx` validates memory actions against the embedded UI contract | PASSED |
| graph-schema-001 | `ui/apps/server/src/optidevMemory.test.ts` covers task/feature/release relations and typed show output | `ui/apps/server/src/optidevRoute.test.ts` validates typed memory responses over the live HTTP route | `ui/apps/web/src/routes/-optidev.browser.tsx` verifies linked memory entities are usable from `/optidev` | PASSED |
| graph-ingest-001 | `ui/apps/server/src/optidevMemory.test.ts` covers deterministic ingestion from docs, release matrices, reports, and session state | `ui/apps/server/src/optidevCliShim.test.ts` validates artifact-driven memory lookup through the shipped CLI | `ui/apps/web/src/routes/-optidev.browser.tsx` validates memory actions from the embedded UI | PASSED |
| graph-decisions-001 | `ui/apps/server/src/optidevMemory.test.ts` covers decision extraction from reports and feature detail rendering | `ui/apps/server/src/optidevRoute.test.ts` validates feature-memory responses with decision details | `ui/apps/web/src/routes/-optidev.browser.tsx` keeps feature-memory interactions stable in the embedded UI | PASSED |
| graph-openloops-001 | `ui/apps/server/src/optidevMemory.test.ts` covers unresolved open-loop extraction and formatting | `ui/apps/server/src/optidevRoute.test.ts` validates `memory_open_loops` over the live HTTP route | `ui/apps/web/src/routes/-optidev.browser.tsx` validates open-loop access inside `/optidev` | PASSED |
| graph-summary-001 | `ui/apps/server/src/optidevMemory.test.ts` covers digest rendering inputs | `ui/apps/server/src/optidevCliShim.test.ts` validates summary output through the shipped CLI | `ui/apps/web/src/routes/-optidev.browser.tsx` keeps the embedded memory summary path stable | PASSED |
| cli-core-001 | `ui/apps/server/src/optidevCli.test.ts` validates native CLI parsing for start/status/logs/projects and shared runtime output shaping | `ui/apps/server/src/optidevCliShim.test.ts` exercises the real root `scripts/optid` shim through Bun | `ui/apps/server/src/optidevCliShim.test.ts` keeps the shipped terminal entrypoint green end-to-end | PASSED |
| cli-memory-001 | `ui/apps/server/src/optidevCli.test.ts` validates native `memory` and `memory show` command handling | `ui/apps/server/src/optidevCliShim.test.ts` validates memory commands through the shipped `scripts/optid` entrypoint | `ui/apps/server/src/optidevCliShim.test.ts` covers `optid memory` and `optid memory show` end-to-end through the real CLI shim | PASSED |
| cli-runtime-002 | `ui/apps/server/src/optidevCli.test.ts` validates native `start`, `resume`, `reset`, and `workspace clone` command behavior | `ui/apps/server/src/optidevCliShim.test.ts` validates the shipped CLI shim for init/runtime flows | `ui/apps/server/src/optidevCliShim.test.ts` keeps manifest lifecycle commands green through the real `optid` entrypoint | PASSED |
| runtime-memory-001 | `ui/apps/server/src/optidevStartup.test.ts` validates digest propagation into startup artifacts and resume flows | `ui/apps/server/src/optidevRoute.test.ts` validates state building with native memory summary over the live route | `ui/apps/web/src/routes/-optidev.browser.tsx` keeps start/restore memory behavior stable in the embedded UI | PASSED |
| graph-release-001 | `ui/apps/server/src/optidevMemory.test.ts` covers release-to-feature linking and typed release lookup | `ui/apps/server/src/optidevCliShim.test.ts` validates release lookup through the shipped CLI | `ui/apps/web/src/routes/-optidev.browser.tsx` keeps release-scoped memory actions stable inside `/optidev` | PASSED |
| plugins-tele-002 | `ui/apps/server/src/optidevPlugins.test.ts` validates native Telegram config/state/event behavior | `ui/apps/server/src/optidevCli.test.ts` validates Telegram start/status/stop through the native CLI runtime | `ui/apps/server/src/optidevCliShim.test.ts` validates Telegram lifecycle through the real `scripts/optid` entrypoint | PASSED |
| runner-api-002 | `ui/apps/server/src/optidevStartup.test.ts` validates runner selection, bootstrap command generation, and `runner.json` persistence | `ui/apps/server/src/optidevCli.test.ts` validates start/go flows through the native CLI runtime | `ui/apps/server/src/optidevCliShim.test.ts` keeps runner bootstrap behavior green through the real shipped shim | PASSED |
| repo-paths-001 | `ui/apps/server/src/optidevMemory.test.ts` validates `tasks-log/` ingestion in the native memory layer | `ui/apps/server/src/optidevCliShim.test.ts` validates memory rebuild through the shipped CLI contract | `ui/apps/web/src/routes/-optidev.browser.tsx` keeps memory-backed UI behavior stable after the report-path migration | PASSED |
| qa-e2e-001 | targeted colocated server tests cover the native runtime slices that feed the UI | `ui/apps/server/src/optidevCliShim.test.ts` and `ui/apps/server/src/optidevRoute.test.ts` validate the shipped CLI and live HTTP route end-to-end enough for the embedded product | `ui/apps/web/src/routes/-optidev.browser.tsx` validates navigation, runtime actions, memory, and plugin flows in the integrated UI | PASSED |
| mux-textual-001 | `ui/apps/server/src/optidevStartup.test.ts` and `ui/apps/server/src/optidevCli.test.ts` validate textual backend selection and generated layout payloads | `ui/apps/server/src/optidevRoute.test.ts` validates native runtime flows with textual session metadata present | `ui/apps/web/src/routes/-optidev.browser.tsx` keeps the fallback `/optidev`-based textual guidance stable | PASSED |
| ui-t3code-001 | `ui/apps/server/src/optidevRoute.test.ts` validates embedded route root resolution and payload shaping inside the forked server, while `ui/apps/web/src/routes/-optidev.browser.tsx` validates the integrated product entry and route behavior | `ui/apps/server/src/optidevRoute.test.ts` exercises `/api/optidev/*` over a live Node HTTP server in the forked server layer | `ui/apps/web/src/routes/-optidev.browser.tsx` validates navigation, runtime actions, memory, and plugin flows inside the forked `t3` UI | PASSED |
| runtime-ts-002 | `ui/apps/server/src/optidevCli.test.ts`, `ui/apps/server/src/optidevNative.test.ts`, `ui/apps/server/src/optidevMemory.test.ts`, `ui/apps/server/src/optidevPersistence.test.ts`, `ui/apps/server/src/optidevLifecycle.test.ts`, `ui/apps/server/src/optidevStartup.test.ts`, and `ui/apps/server/src/optidevPlugins.test.ts` validate the native TS/Bun runtime core for CLI, state, memory, persistence, lifecycle, startup, and plugin behavior | `ui/apps/server/src/optidevCliShim.test.ts` and `ui/apps/server/src/optidevRoute.test.ts` validate both the shipped CLI shim and native-only `/api/optidev/*` routing without Python fallback | `ui/apps/web/src/routes/-optidev.browser.tsx` keeps `/optidev` behavior stable while the server runtime and shipped CLI stay native-only | PASSED |
| runtime-ts-003 | `ui/apps/server/src/optidevMemory.test.ts`, `ui/apps/server/src/optidevStartup.test.ts`, and `ui/apps/server/src/optidevRoute.test.ts` validate the native-only repository/runtime markers after Python retirement | `ui/apps/server/src/optidevCliShim.test.ts` and `ui/apps/server/src/optidevRoute.test.ts` validate the shipped Bun CLI and live route without any Python package present | `ui/apps/web/src/routes/-optidev.browser.tsx` keeps the embedded product behavior stable after legacy cleanup | PASSED |
| repo-docs-002 | `scripts/verify-doc-layout.sh` validates that current release docs remain active and superseded docs no longer occupy active paths | `scripts/verify-doc-layout.sh` validates obsolete materials were moved under `docs/obsolete/` with expected structure | manual README, AGENTS, and guide path audit plus `scripts/verify-doc-layout.sh` confirm contributors are pointed to active docs first | PASSED |
| repo-upstream-001 | `scripts/t3code-sync.test.mjs` validates transient path filtering, vendored metadata lifecycle, successful overlay replay, and dirty-tree protection | `scripts/t3code-sync.test.mjs` exercises the refresh workflow against temporary git repos that simulate upstream `t3code` evolution and local OptiDev overlay changes | `node scripts/t3code-sync.mjs status` plus `node scripts/t3code-sync.mjs refresh --target-ref main --dry-run --allow-dirty` validate the real vendored workflow against the live upstream ref and report concrete replay conflicts | PASSED |
| repo-upstream-002 | `scripts/t3code-sync.test.mjs` validates hotspot-specific merge shaping for `wsServer.ts`, `Sidebar.tsx`, and `vitest.browser.config.ts` through the repository-owned resolvers | `scripts/t3code-sync.test.mjs` exercises a temporary upstream repo whose refresh would otherwise conflict in the three hotspot files and verifies that refresh completes cleanly | `node scripts/t3code-sync.mjs refresh --target-ref main --dry-run --allow-dirty` validates on 2026-03-12 that the real upstream refresh now auto-resolves the current hotspot conflicts and reports them in `resolvedConflicts` | PASSED |
| repo-upstream-003 | `scripts/optid-launcher-lib.test.mjs` validates explicit `optid t3code ...` routing, default subcommand resolution, and flag forwarding without colliding with UI/native CLI routes | `node scripts/optid-runner.mjs --root /home/dimkk/new-proj/optistart t3code status` validates that the shipped launcher delegates vendored maintenance commands into `scripts/t3code-sync.mjs` | `node scripts/optid-runner.mjs --root /home/dimkk/new-proj/optistart t3code refresh --target-ref main --dry-run --allow-dirty` validates the operator-facing upstream refresh path against the live upstream ref | PASSED |
| repo-release-001 | `scripts/release-lib.test.mjs` validates version parsing, semver bumping, tag normalization, and manifest-driven package sync from one source of truth | `node scripts/prepare-release.mjs show` validates repository release metadata resolution through the shipped prep CLI | `.github/workflows/release-bump.yml` and `.github/workflows/release-publish.yml` define merge-to-main bumping, tagging, and release publication from repository-owned automation | PASSED |
| repo-release-002 | `scripts/run-main-pr-checks.sh` validates that repository-owned script tests, launcher checks, doc layout, vendored lint/test suites, and browser route coverage stay in one maintained validation surface | `.github/workflows/pr-main-checks.yml` runs the shared validation script on pull requests targeting `main`, while `.github/workflows/release-bump.yml` reuses the same script before tagging a merge commit | GitHub Actions PR runs for `main` now execute the repository-owned validation suite before merge, covering script tests, doc layout, vendored lint/tests, and the browser route suite | PASSED |
| cli-update-001 | `scripts/release-lib.test.mjs` validates semver ordering, latest-tag parsing, and update suggestion shaping | `OPTIDEV_HOME=/tmp/optid-update-check-test bun scripts/optid-update-check.mjs --force` validates installed-version update checks and cache writes without blocking startup | `scripts/optid-runner.mjs` integrates cached latest-tag checks into launcher startup and surfaces upgrade suggestions when a newer release is available | PASSED |
| cli-launch-001 | `scripts/optid-launcher-lib.test.mjs` validates local versus installed launcher behavior, no-arg UI startup, and version routing | `bash -n scripts/install.sh scripts/optid`, `node scripts/prepare-release.mjs show`, and `bun scripts/optid-runner.mjs --root /home/dimkk/new-proj/optistart --version` validate launcher/install entrypoints and shipped version exposure | `bun scripts/optid-runner.mjs --root /home/dimkk/new-proj/optistart status` validates that the stable launcher still delegates existing native OptiDev CLI flows while `optid` default UI startup is now available | PASSED |
| ui-shell-002 | `ui/apps/server/src/optidevNative.test.ts` validates the structured native session payload consumed by the shell workspace and `ui/apps/server/src/optidevRoute.test.ts` validates integrated route state shaping | `ui/apps/server/src/optidevRoute.test.ts` exercises the shell-mounted `/api/optidev/state` contract over a live HTTP server | `ui/apps/web/src/routes/-optidev.browser.tsx` validates shell navigation into `/optidev`, stable tab rendering, and non-stuck status presentation | PASSED |
| repo-files-002 | `ui/apps/server/src/optidevFiles.test.ts` validates safe scope resolution, directory ordering, file classification, and traversal guards | `ui/apps/server/src/optidevRoute.test.ts` validates repository file listing and preview endpoints over a live HTTP server | `ui/apps/web/src/routes/-optidev.browser.tsx` validates repository browsing plus markdown render/source behavior in the Files tab | PASSED |
| runtime-session-002 | `ui/apps/server/src/optidevNative.test.ts` validates structured session details and merged state payloads for the session tab | `ui/apps/server/src/optidevRoute.test.ts` validates native session state delivery through `/api/optidev/state` | `ui/apps/web/src/routes/-optidev.browser.tsx` validates runtime controls, memory/open-loop access, and session status updates from the OptiDev tab | PASSED |
| plugins-config-001 | `ui/apps/server/src/optidevConfig.test.ts` and `ui/apps/server/src/optidevFiles.test.ts` validate Telegram config persistence plus scoped plugin file writes | `ui/apps/server/src/optidevRoute.test.ts` validates plugin file save and Telegram config endpoints over the live HTTP route | `ui/apps/web/src/routes/-optidev.browser.tsx` validates plugin file creation/editing and Telegram settings updates from the Plugins tab | PASSED |
